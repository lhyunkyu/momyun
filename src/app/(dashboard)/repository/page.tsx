import { RepositorySection } from '@/components/sections/RepositorySection'

export const metadata = { title: '레포지토리 — 모면' }

export default function RepositoryPage() {
  return <RepositorySection />
}
